import React from 'react';
import TaskItem from './TaskItem';

// TODO: placeholder only — remove once real task data is wired in
const SampleTask: React.FC = () => {
  return <div /> // Make placeholder no longer create type errors
  // return <TaskItem label="Tom's coding contribution" />;
};

export default SampleTask;